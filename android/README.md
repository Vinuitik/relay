# android

Native Android app (not PWA — see ARCHITECTURE.md for why). Holds the known-runners list
(hostname + key per runner), browses projects/sessions per runner, sends wake/stop-containers
commands, and surfaces per-session job-done notifications.

Not yet implemented.
