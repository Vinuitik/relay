# android

Native Android app (not PWA — see ARCHITECTURE.md for why). Holds the known-nodes list
(hostname + key per node), browses projects/sessions per node, sends wake/stop-containers
commands, and surfaces per-session job-done notifications.

Not yet implemented.
